<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
        <%- include('header') %>

        <h2>Daftar Transactions</h2>
        <ul>
            <% if(transactions.length === 0) { %>
                <p>data produk kosong</p>
            <%} else { %>
                <% transactions.forEach((item) => { %>
                    <li>Nama Custommer : <%= item.name %> </li>
                    <li>Product : <%= item.product %> </li>
                    <li>Harga : <%= item.price %> </li>
                    <li>Jumlah : <%= item.qty %> </li>
                    <li>Total : <%= item.total %> </li>
                    <hr />
               <% }) %>
            <%}%>
        </ul>


        <%- include('footer') %>

</body>
</html>